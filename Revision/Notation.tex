\renewcommand{\DeprFac}{(1-\delta)}
\renewcommand{\PDies}{\mathrm{d}}
\renewcommand{\PLives}{(1-\mathrm{d})}
\newcommand{\cFuncLev}{\textbf{c}} % I could not see why we wanted a hat over the consumption function with all state variables -- especially since later we use a hat for the signal extraction problem
